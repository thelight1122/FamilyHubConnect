export const emptyFinance = Object.freeze({
  totalSavings: 0,
  walletBalance: 0,
  points: 0,
  recentActivity: [],
  loans: [],
});

export const emptySports = Object.freeze({
  teams: [],
  contacts: [],
  equipment: [],
  schedule: [],
});

export const emptyConstitution = Object.freeze({
  mission: '',
  values: [],
  rules: [],
});

export const emptyAppeal = Object.freeze({
  currentMeasure: null,
  proposedAlternative: '',
});

export const emptyGovernance = Object.freeze({
  activeMediation: [],
  circle: [],
  archive: [],
});

export const emptyCreatorStudio = Object.freeze({
  stories: [],
  polls: [],
  voiceMemos: [],
});

export const emptyMaintenance = Object.freeze({
  items: [],
});
